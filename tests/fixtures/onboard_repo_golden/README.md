# Onboard Golden Fixture

Stable fixture repo used by piia-engram `onboard repo` acceptance tests. The
dependencies (npm + Python) and files here are deliberately fixed anchors — do
not edit casually; acceptance transcripts assert against them.
