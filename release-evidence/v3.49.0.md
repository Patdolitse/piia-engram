# Release evidence - v3.49.0

- self-review: passed
- codex-review: passed
- claude-review: passed
- tests: passed
- sanitize: passed
- publish-allowlist: passed
- package-build: passed
- artifact-private-scan: passed
- twine-check: passed
- eval-gate: passed
- negative-control: passed
- field-assertion-audit: passed

## Evidence Notes

- Self review - release scope reviewed as a local release candidate, not a publish action.
- Codex review - version-bearing surfaces, public facts, changelogs, packaging evidence, and release-gate requirements reviewed by Codex.
- Claude review - Claude Code 2.1.158 narrow English acceptance review returned PASS with no blocking issues for the v3.49.0 RC diff.
- Tests - full suite returned 2576 passed, 8 skipped, 4 warnings; collect-only returned 2584 tests collected; targeted packaging/public fact checks returned 42 passed.
- Sanitize - final strict release sanitize check must report high=0 and warn=0 before publish.
- Publish allowlist - final default-deny publish allowlist check must pass before publish.
- Package build - local wheel and sdist built in `E:\Temp\engram-build-v3.49.0-final-20260604203912`.
- Artifact private scan - `scripts/check_release_artifact_private_terms.py <build-dir> --strict` reported high=0 and warn=0 for the built artifacts.
- Twine check - `python -m twine check <build-dir>\*` passed for both artifacts.
- Eval gate - MCIC v1 synthetic benchmark and tests were added for cross-client identity-continuity evidence; this is a memory-layer signal benchmark, not a live model compliance claim.
- Negative control - new checks were observed red before implementation for the import materialization flag/behavior and for the missing MCIC benchmark module.
- Field assertion audit - version-chain materialization and MCIC tests assert metadata-only surfaces and avoid leaking raw body/secret text in review/benchmark payloads.

## English

Engram v3.49.0 turns the recent continuity work into a releasable local checkpoint. It adds opt-in import version-chain materialization for same-key divergent lessons and decisions, keeping the default import path conservative while giving owners an explicit way to preserve conflict history as active HEAD entries. It also adds MCIC v1, a synthetic metadata-only benchmark for multi-client identity continuity, so future claims can separate memory-layer signal availability from live model compliance.

Highlights:
- `engram import <backup.json> --apply --yes --materialize-version-chain` can materialize reviewed import conflicts as version-chain entries.
- MCIC v1 covers explicit recall, implicit personalization signals, adversarial false-premise guard signals, safety boundaries, version-chain HEAD behavior, negative controls, and provenance.
- Public facts are refreshed to v3.49.0 with 2576 passed, 8 skipped, and 2584 collected tests.

Verification:
- package build: passed
- artifact private scan: high=0, warn=0
- twine check: passed
- Claude narrow acceptance review: PASS, no blocking issues

## Chinese

Engram v3.49.0 把最近的跨工具连续性工作收口成本地发布候选检查点。本版新增显式开启的导入版本链实体化：对于同 key 但内容分叉的 lesson / decision，默认导入仍保持保守合并；只有所有者明确添加 `--materialize-version-chain` 时，才会把已审阅的冲突保留为新的 active HEAD 版本链条目。同时新增 MCIC v1 合成基准，用 metadata-only 的方式验证多客户端身份连续性信号，避免把“记忆层能否提供信号”和“现场模型是否一定遵守”混在一起。

重点：
- `engram import <backup.json> --apply --yes --materialize-version-chain` 可将审阅过的导入冲突实体化为版本链条目。
- MCIC v1 覆盖显式召回、隐式个性化信号、反事实诱导防护信号、安全边界、版本链 HEAD、负对照和 provenance。
- 公开事实刷新到 v3.49.0：2576 passed，8 skipped，2584 collected。

验证：
- 本地包构建：通过
- artifact 私有词扫描：high=0，warn=0
- twine check：通过
- Claude 窄范围验收审计：PASS，无阻塞问题
