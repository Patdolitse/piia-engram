# Release evidence - v3.49.0

- self-review: passed
- codex-review: passed
- claude-review: passed
- tests: passed
- sanitize: passed
- publish-allowlist: passed
- package-build: passed
- artifact-private-scan: passed
- twine-check: passed
- eval-gate: passed
- negative-control: passed
- field-assertion-audit: passed

## Evidence Notes

- Self review - release scope reviewed as a local release candidate, not a publish action.
- Codex review - version-bearing surfaces, public facts, changelogs, packaging evidence, and release-gate requirements reviewed by Codex.
- Claude review - Claude Code 2.1.158 final narrow English acceptance review returned PASS with no blocking issues for the refreshed v3.49.0 release-prep diff.
- Tests - isolated full suite returned 2595 passed, 8 skipped, 4 warnings; collect-only returned 2603 tests collected.
- Sanitize - internal release sanitize check reported high=0 and warn=0.
- Publish allowlist - default-deny publish allowlist check passed for 318 tracked files across 77 allowlist patterns.
- Package build - local wheel and sdist built in `E:\Temp\engram-build-v3.49.0-final-20260604230517`.
- Artifact private scan - `scripts/check_release_artifact_private_terms.py <build-dir> --strict` reported high=0 and warn=0 for the built artifacts.
- Twine check - `python -m twine check <build-dir>\*` passed for both artifacts.
- Release auth preflight - GitHub CLI auth, mcp-publisher availability, `.mcp/server.json` validation, MCP version match, twine availability, and PyPI credential source checks passed.
- Eval gate - MCIC v1 synthetic benchmark and reusable client-validation evidence harness were added for cross-client identity-continuity evidence; this is a memory-layer signal/evidence benchmark, not a live model compliance claim.
- Negative control - new checks were observed red before implementation for the import materialization flag/behavior and for the missing MCIC benchmark module.
- Field assertion audit - version-chain materialization, MCIC, and client-validation tests assert metadata-only/public-safe surfaces and avoid leaking raw body/secret text in review/benchmark payloads.

## English

Engram v3.49.0 turns the recent continuity work into a releasable local checkpoint. It adds opt-in import version-chain materialization for same-key divergent lessons and decisions, keeping the default import path conservative while giving owners an explicit way to preserve conflict history as active HEAD entries. It also adds MCIC v1 and a reusable client-validation evidence harness, so future claims can separate memory-layer signal availability, copied-store client evidence, and live model compliance.

Highlights:
- `engram import <backup.json> --apply --yes --materialize-version-chain` can materialize reviewed import conflicts as version-chain entries.
- MCIC v1 covers explicit recall, implicit personalization signals, adversarial false-premise guard signals, safety boundaries, version-chain HEAD behavior, negative controls, and provenance.
- `scripts/run_client_validation.py` records isolated client-validation evidence for Hermes, OpenClaw, Cursor, and future MCP hosts without touching the user's live Engram store.
- The Cursor plugin display name now uses `piia-engram` for package and registry consistency.
- Public facts are refreshed to v3.49.0 with 2595 passed, 8 skipped, and 2603 collected tests.

Verification:
- package build: passed
- artifact private scan: high=0, warn=0
- twine check: passed
- release authorization preflight: passed
- Claude narrow acceptance review: PASS, no blocking issues

## Chinese

Engram v3.49.0 把最近的跨工具连续性工作收口成本地发布候选检查点。本版新增显式开启的导入版本链实体化：对于同 key 但内容分叉的 lesson / decision，默认导入仍保持保守合并；只有所有者明确添加 `--materialize-version-chain` 时，才会把已审阅的冲突保留为新的 active HEAD 版本链条目。同时新增 MCIC v1 合成基准和可复用的客户端验证证据脚手架，用 metadata-only / public-safe 的方式区分“记忆层能否提供信号”、“复制数据目录的客户端证据是否成立”和“现场模型是否一定遵守”。

重点：
- `engram import <backup.json> --apply --yes --materialize-version-chain` 可将审阅过的导入冲突实体化为版本链条目。
- MCIC v1 覆盖显式召回、隐式个性化信号、反事实诱导防护信号、安全边界、版本链 HEAD、负对照和 provenance。
- `scripts/run_client_validation.py` 可记录 Hermes、OpenClaw、Cursor 和后续 MCP host 的隔离客户端验证证据，不触碰用户真实 Engram 数据目录。
- Cursor 插件显示名统一为 `piia-engram`，与包名和 registry 身份一致。
- 公开事实刷新到 v3.49.0：2595 passed，8 skipped，2603 collected。

验证：
- 本地包构建：通过
- artifact 私有词扫描：high=0，warn=0
- twine check：通过
- 发布授权预检：通过
- Claude 窄范围验收审计：PASS，无阻塞问题
