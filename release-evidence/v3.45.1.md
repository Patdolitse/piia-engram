# Release Evidence - v3.45.1

- self-review: passed       # Codex reviewed the CI failure, root cause, patch diff, version/docs consistency, and release-gate readiness.
- codex-review: passed      # Codex local review covered the clean-checkout import failure, package manifest coverage, test evidence, sanitize scans, package build, and twine metadata validation.
- claude-review: passed     # Claude narrow read-only release acceptance for the v3.45.x release path returned PASS with no release blockers; broad open-permission Claude remained inconclusive due timeout.
- tests: passed             # Full suite 2020 passed, 1 skipped, 4 expected engram_core deprecation warnings.
- sanitize: passed          # scripts/release_sanitize_check.py --strict and --commit-messages --strict returned high=0 warn=0.
- publish-allowlist: passed # scripts/check_publish_allowlist.py confirmed tracked public files are covered by .publishallow, including MANIFEST.in and demos/.
- package-build: passed     # python -m build --outdir E:\Temp\engram-build-v3.45.1 built wheel and sdist.
- twine-check: passed       # python -m twine check E:\Temp\engram-build-v3.45.1\* passed for both artifacts.
- eval-gate: n/a            # This patch changes CI/package import coverage only; no retrieval or extraction-quality behavior changed.
- negative-control: n/a     # No security-sensitive governance/encryption/redaction behavior changed in this patch.
- field-assertion-audit: n/a # No security-sensitive storage/encryption/redaction module fields were changed in this patch.

## Scope

Patch release after v3.45.0:

- Fixed the GitHub Actions collection failure caused by `tests/test_cross_tool_resume_benchmark.py` importing `demos.cross_tool_resume_benchmark` from a clean CI checkout.
- Added `demos/__init__.py` so the top-level demo namespace is importable.
- Added `MANIFEST.in` coverage for `demos/*.py` so the sdist includes the demo module required by the public test suite.
- Bumped package, MCP registry manifest, Claude plugin manifest, docs, changelogs, and public-positioning tests to 3.45.1.

## Evidence Summary

- CI failure root cause in v3.45.0: `ModuleNotFoundError: No module named 'demos'` during collection of `tests/test_cross_tool_resume_benchmark.py`.
- Clean import regression: `python -m pytest tests/test_cross_tool_resume_benchmark.py -q` without `PYTHONPATH` returned `4 passed`.
- Targeted release-prep suite: `59 passed`.
- Full suite: `2020 passed, 1 skipped, 4 warnings in 190.31s`.
- Test collection: `2021 tests collected in 1.69s`.
- Working-tree sanitization: `high=0 warn=0`.
- Commit-message sanitization: `high=0 warn=0`.
- Package build: `Successfully built piia_engram-3.45.1.tar.gz and piia_engram-3.45.1-py3-none-any.whl` into `E:\Temp\engram-build-v3.45.1`.
- Sdist verification: `piia_engram-3.45.1/demos/__init__.py` and `piia_engram-3.45.1/demos/cross_tool_resume_benchmark.py` are present.
- Twine: `PASSED` for both `piia_engram-3.45.1-py3-none-any.whl` and `piia_engram-3.45.1.tar.gz`.
- Claude acceptance: PASS for the narrow read-only release acceptance path.
- Claude open-permission experiment: INCONCLUSIVE; broad Opus open-permission release review still timed out and produced no report, so the timeout is not solved by permissions alone.

## Release Notes Draft

v3.45.1 is a CI and packaging patch release for v3.45.0.

It fixes the clean-checkout GitHub Actions failure where the cross-tool resume benchmark test imported the top-level `demos` namespace before that namespace was packaged/importable in CI. The patch adds a package initializer for `demos/` and includes the demo scripts in the source distribution manifest, keeping local, CI, and packaged test behavior aligned.

No runtime behavior, security-sensitive governance gate, storage encryption path, or user data migration behavior changed in this patch.

No publishing, tag creation, registry update, or other irreversible action is authorized by this evidence file alone.
