# Release Evidence - v3.47.1

- self-review: passed       # Codex reviewed public fact manifest, CI/publish gate wiring, version surfaces, changelog entries, README current-state tables, and release notes.
- codex-review: passed      # Codex independently verified the public truth guard, deterministic MCP tool count helper, workflow enforcement, release boundary, and no-public-action gates before release prep commit.
- claude-review: passed     # Claude D+ implementation and review for Public Truth Sync returned approve across implementation/test, security/privacy/governance, and docs/public-truth dimensions; Codex reviewed and tightened ASCII/source hygiene.
- tests: passed             # Clean full suite with ENGRAM_DIR cleared: 2415 passed, 8 skipped, 4 expected engram_core deprecation warnings in 152.78s; collect-only: 2423 tests collected.
- sanitize: passed          # scripts/release_sanitize_check.py --internal --strict and staged strict scans returned high=0 warn=0.
- publish-allowlist: passed # scripts/check_publish_allowlist.py confirmed all tracked files are covered by .publishallow.
- package-build: passed     # python -m build --outdir E:\Temp\engram-build-v3.47.1 built wheel and sdist for 3.47.1.
- artifact-private-scan: passed # scripts/check_release_artifact_private_terms.py E:\Temp\engram-build-v3.47.1 --strict returned high=0 warn=0.
- twine-check: passed       # python -m twine check E:\Temp\engram-build-v3.47.1\* PASSED for both artifacts.
- eval-gate: n/a            # This release changes public fact/release guard automation only; it does not change retrieval ranking, extraction, or benchmark-quality behavior.
- negative-control: passed  # Public fact sync tests include injected stale test-count/tool-count/version failures, proving the guard blocks drift instead of merely passing the happy path.
- field-assertion-audit: n/a # No encryption, redaction, telemetry payload, or permission-gated data field changed.

## Scope

v3.47.1 is a public truth sync and release-train hardening patch. It prevents
Engram's current-state public docs and package metadata from drifting apart by
adding a machine-readable fact manifest, a fail-closed public fact guard, a
deterministic MCP tool counter, and CI/publish workflow enforcement.

This release performs no telemetry schema change, no remote D1/Worker change, no
permission-profile enforcement change, no user-data migration, and no new MCP
tool surface.

## Changes

- Bumped package metadata and `__version__` to 3.47.1 across `pyproject.toml`,
  `src/piia_engram/__init__.py`, `.mcp/server.json`, `.claude-plugin/plugin.json`,
  `glama.yaml`, and current-state docs.
- Added `docs/public-facts.json` as the local development source of truth for:
  version, full-suite test count, collected-test count, MCP tool split, and
  telemetry default posture.
- Added `scripts/check_public_fact_sync.py` and wired it into:
  - `.github/workflows/ci.yml`
  - `.github/workflows/publish.yml`
  - `docs/runbooks/release-build-check.md`
- Added `scripts/count_mcp_tools.py`, which re-derives the 80/16/64 MCP tool
  split from source via AST without importing the package.
- Updated README and README.zh-CN current-state tables to:
  - 2415 passed
  - 8 skipped
  - 2423 collected
- Added tests proving stale public facts fail closed while historical changelog
  and release-evidence entries remain valid historical records.

## Evidence Summary

- Public fact sync: `scripts/check_public_fact_sync.py --json` returned OK for
  v3.47.1 with 2415 passed / 8 skipped / 2423 collected and 80/16/64 MCP tools.
- MCP tool counter: `scripts/count_mcp_tools.py --json` returned
  `{"total": 80, "core": 16, "advanced": 64}`.
- Guard + packaging tests: `46 passed`.
- Full suite with `ENGRAM_DIR` cleared: `2415 passed, 8 skipped, 4 warnings`.
- Collect-only: `2423 tests collected`.
- Publish allowlist: all tracked files covered.
- Source/private-term sanitize: high=0 warn=0.
- Build: `piia_engram-3.47.1.tar.gz` and `piia_engram-3.47.1-py3-none-any.whl`.
- Artifact private-term scan: high=0 warn=0.
- Twine: PASSED for both v3.47.1 artifacts.
- Release gate: `scripts/check_release_gate.py --version 3.47.1` passed.

## Release Notes Draft

### English

Engram v3.47.1 is a public truth sync and release-train hardening patch.

It adds a machine-readable `docs/public-facts.json` manifest for the current
development facts Engram publishes about itself: version, test count, MCP tool
split, and telemetry defaults. A new `scripts/check_public_fact_sync.py` guard
now runs in CI and before PyPI publishing, so README, manifests, and release
metadata cannot silently drift from the manifest. The release also adds a
deterministic `scripts/count_mcp_tools.py` helper for re-deriving the 80/16/64
MCP tool split from source.

No telemetry schema, remote service, permission enforcement, user-data
migration, or new MCP tool surface changes in this patch.

### Chinese / 中文

Engram v3.47.1 是公开事实同步与发布链加固补丁。

本版本新增 `docs/public-facts.json`，作为 Engram 对外当前事实的机器可读单一来源，包括版本号、测试数、MCP 工具分层和 telemetry 默认状态。新的 `scripts/check_public_fact_sync.py` 已接入 CI 和 PyPI 发布流程，防止 README、manifest 和发布元数据在发布前再次漂移。版本还新增 `scripts/count_mcp_tools.py`，用于从源码确定性计算 80/16/64 的 MCP 工具分层。

本补丁不改变 telemetry schema、不改远端服务、不启用权限 enforcement、不迁移用户数据，也不新增 MCP 工具面。
