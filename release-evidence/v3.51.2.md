# Release evidence - v3.51.2

- self-review: passed
- codex-review: passed
- claude-review: passed
- tests: passed
- sanitize: passed
- publish-allowlist: passed
- package-build: passed
- artifact-private-scan: passed
- twine-check: passed
- eval-gate: passed
- negative-control: passed
- field-assertion-audit: passed

## Evidence Notes

- Self review - release scope reviewed as a patch release candidate after the
  Playbook contract / outcome / usage-policy hardening and the
  Playbook-to-tools-registry bridge landed.
- Codex review - version-bearing surfaces, changelog entries, public facts,
  Playbook required-tool persistence boundaries, runtime-only resolved tool
  output, and release evidence were reviewed during release prep.
- Claude review - Claude backlog triage recommended releasing the pushed
  `4ccb81b` Playbook-to-tools-registry feature as a clean patch boundary before
  starting the next context-governance feature batch.
- Tests - full suite returned 2906 passed, 2 skipped, and 4 warnings;
  collect-only returned 2908 tests collected.
- Negative control - required tool version uncertainty is covered so an
  unknown registered tool version does not falsely satisfy `min_version`.
- Field assertion audit - resolved local tool paths remain runtime-only outputs
  from `prepare_playbook_execution`; they are not persisted into Playbooks or
  execution-plan files.
- Memory eval suite - `scripts/run_memory_evals.py --json` passed: recall
  baseline 8/8, recall held-out 10/10, admission baseline 7/7, admission
  held-out 8/8, forbidden leak rate 0.0, negative false-positive rate 0.0.
- Sanitize - internal strict release sanitize check reported high=0 and warn=0.
- Publish allowlist - default-deny publish allowlist check passed for tracked
  public files.
- Public fact sync - current-state public facts agree with
  `docs/public-facts.json`.
- Public claim drift - Markdown-facing quantified surfaces passed the public
  claim drift sweep.
- Public trust claims - SECURITY/README/PRIVACY/telemetry prose claims passed
  the network, telemetry, plaintext, optional-encryption, and endpoint
  consistency guard.
- Generated export redaction - synthetic Engram data rendered identity card,
  knowledge report, and AGENTS export surfaces; all scanned clean with
  high=0/warn=0.
- Artifact scan - `dist/` was cleaned to only v3.51.2 artifacts before the
  final scan; strict artifact private-term scan reported high=0 and warn=0.
- Package build - built `piia_engram-3.51.2.tar.gz` and
  `piia_engram-3.51.2-py3-none-any.whl`.
- Twine check - current v3.51.2 wheel and sdist both passed.

## English

Engram v3.51.2 is a Playbook execution-governance patch. It keeps Playbooks as
portable process knowledge while letting agents resolve local tool availability
through the tools registry at runtime.

Highlights:
- Added canonical `required_tools` metadata for Playbooks, with `tool_refs` kept
  as an input alias.
- Extended `prepare_playbook_execution` to return runtime-only
  `resolved_tools`, `tools_ready`, and `missing_tools`.
- Kept resolved local paths out of stored Playbooks and execution-plan files.
- Preserved tool dependencies through Playbook update and merge flows.
- Surfaced unknown tool versions as unknown / unsatisfied for `min_version`
  checks instead of giving false confidence.

Verification:
- full test suite: 2906 passed, 2 skipped, 4 warnings
- collect-only count: 2908 tests collected
- MCP tool counter: 83 total, 17 core, 66 advanced
- CI on pushed feature commit: passed
- memory eval suite: passed
- public fact sync: passed
- public claim drift: passed
- public trust claims: passed
- generated export redaction: passed
- sanitize check result: high=0, warn=0
- publish allowlist: passed
- package build: passed
- artifact private scan result: high=0, warn=0
- twine check result: passed

## Chinese

Engram v3.51.2 是一次 Playbook 执行治理补丁。它让 Playbook 继续保持可携带的流程知识，
同时允许 AI 在执行前通过工具图谱运行时解析本地工具可用性。

重点：
- 新增 Playbook canonical `required_tools` 元数据，并保留 `tool_refs` 作为输入别名。
- `prepare_playbook_execution` 现在返回运行时 `resolved_tools`、`tools_ready` 和
  `missing_tools`。
- 本机解析出的绝对路径不会写入已保存 Playbook 或 execution plan。
- Playbook update / merge 流程会保留工具依赖。
- 对 `min_version` 检查来说，未知工具版本会明确显示为 unknown / unsatisfied，
  不再给出误导性的满足状态。

验证：
- 完整测试：2906 passed，2 skipped，4 warnings
- collect-only 数量：2908 tests collected
- MCP 工具计数：83 total，17 core，66 advanced
- 已推送 feature commit 的 GitHub CI：通过
- memory eval suite：通过
- public fact sync：通过
- public claim drift：通过
- public trust claims：通过
- 动态导出脱敏：通过
- 脱敏扫描结果：high=0，warn=0
- publish allowlist：通过
- package build：通过
- artifact private scan 结果：high=0，warn=0
- twine check：通过
