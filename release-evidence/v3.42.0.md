# Release Evidence - v3.42.0

- self-review: passed       # Codex implemented and reviewed the trust/resume/portability foundation, version/docs/evidence consistency, and release gates.
- codex-review: passed      # Codex subagents independently reviewed engineering consistency and security/privacy; initial P1 findings were fixed and final re-review passed.
- claude-review: passed     # Claude Code Opus 4.8 high read-only acceptance and final release-scope re-review passed after documentation consistency fixes.
- tests: passed             # Full suite 1826 passed, 1 skipped, 4 expected engram_core deprecation warnings.
- sanitize: passed          # scripts/release_sanitize_check.py --staged --internal --strict returned high=0 warn=0 during release prep.
- publish-allowlist: passed # scripts/check_publish_allowlist.py --staged confirmed all 172 tracked files were covered; new public plan and release-evidence docs are covered.
- package-build: passed     # python -m build --outdir E:\Temp\engram-build-v3.42.0-releaseprep built wheel and sdist.
- twine-check: passed       # python -m twine check E:\Temp\engram-build-v3.42.0-releaseprep\* passed for both artifacts.
- eval-gate: n/a            # No search ranking, retrieval scoring, or quality-evaluation algorithm changed.
- negative-control: passed  # TDD red checks proved trust self-downgrade, non-fix doctor writeback, and staging promotion regressions fail before fixes.
- field-assertion-audit: passed # Security-sensitive trust/diagnostic fields are covered by metadata-only/no-body/no-write assertions and subagent/Claude review.

## Scope

Release chain after v3.41.0:

- `d92fb61 fix(setup): harden entry help and encoding reads`
- `d05a713 fix(recovery): add safe json candidate recovery`
- `36c626b fix(setup): preserve mcp client config formats`
- `b439a50 feat(trust): add resume and config integrity foundation`

Covered changes:

- CLI entry help avoids initialization side effects, Windows UTF-8 output is decoded reliably, and JSON readers accept UTF-8 BOM.
- Setup wizard preserves MCP client config formats and nonstandard server keys.
- Codex TOML config writer dispatch and Windows TOML path escaping are covered.
- Safe JSON recovery and retention dry-run report metadata only and never restore live stores automatically.
- `get_resume_brief()` starts with a 30-second handoff and trust note.
- Trust-mode metadata is server-derived and cannot be downgraded by caller-supplied fields.
- `doctor` non-`--fix` continuity checks avoid legacy knowledge-file backfill side effects.
- Terminal `engram doctor` reports config integrity with counts/hashes only.

## Evidence Summary

- Full suite: `1826 passed, 1 skipped, 4 warnings in 177.89s`.
- Stage-focused suite: `535 passed, 1 skipped`.
- Core/setup suite after trust and doctor fixes: `508 passed`.
- Publish allowlist: `[OK] all 172 tracked files are covered by .publishallow (46 patterns)`.
- Internal staged sanitize: `high=0 warn=0`.
- Build: `Successfully built piia_engram-3.42.0.tar.gz and piia_engram-3.42.0-py3-none-any.whl` into `E:\Temp\engram-build-v3.42.0-releaseprep`.
- Twine: `PASSED` for both `piia_engram-3.42.0-py3-none-any.whl` and `piia_engram-3.42.0.tar.gz`.
- Codex subagent review: PASS after fixing trust self-downgrade, doctor writeback, and promotion metadata issues.
- Claude Code acceptance: PASS after adding the new plan to `.publishallow` and aligning documentation with current access-based promotion behavior.

## Release Notes Draft

This release strengthens Engram's local-first trust and continuity foundations. It adds a safer recovery retention dry-run, a 30-second cross-tool resume handoff, derived trust metadata for knowledge entries, and metadata-only configuration integrity diagnostics in `engram doctor`.

No publishing, tag creation, or registry update is authorized by this evidence file alone.
