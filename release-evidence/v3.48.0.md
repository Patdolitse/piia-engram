# Release Evidence - v3.48.0

- self-review: passed       # Claude reviewed version surfaces (pyproject, __init__, .mcp/server.json, .claude-plugin/plugin.json, glama.yaml, docs), bilingual changelog entries, public-fact manifest sync, and the CLI/owner-only apply surfaces before release prep.
- codex-review: passed      # E+ substitution: under the E+ mechanism (Codex budget unavailable), an independent DeepSeek final audit + a Claude security/privacy subagent stand in for Codex review. DeepSeek verdict: APPROVE_WITH_NITS, 0 blocking issues, across all 9 audit items (goal match, meaningful tests, privacy/governance not weakened, no overclaim, no irreversible action, metadata-only invariant, no agent-facing apply bypass, telemetry intact, public-fact counts consistent).
- claude-review: passed     # Claude code-review subagent returned PASS: implementation matches goal; metadata-only invariant preserved on dashboard/reconcile/merge surfaces (counts/ids/labels only, no bodies/secrets/paths); tests meaningful.
- tests: passed             # Clean full suite with ENGRAM_DIR cleared: 2469 passed, 8 skipped, 2477 collected.
- sanitize: passed          # scripts/release_sanitize_check.py --internal --strict returned high=0 warn=0 on the working tree.
- publish-allowlist: passed # scripts/check_publish_allowlist.py confirmed all 295 tracked files are covered by .publishallow (71 patterns).
- package-build: passed     # python -m build --outdir E:\Temp\engram-build-v3.48.0 built wheel and sdist for 3.48.0.
- artifact-private-scan: passed # scripts/check_release_artifact_private_terms.py E:\Temp\engram-build-v3.48.0 --strict returned high=0 warn=0.
- twine-check: passed       # python -m twine check E:\Temp\engram-build-v3.48.0\* PASSED for both artifacts.
- eval-gate: n/a            # This release changes CLI/owner-only apply surfaces and readiness metadata only; it does not change retrieval ranking, extraction, or benchmark-quality behavior.
- negative-control: passed  # New tests assert the metadata-only / no-mutation invariants fail closed: dashboard/reconcile/merge JSON must not contain a known SECRET sentinel, and the conflict preview leaves the active-decision count unchanged (proving it never imports/supersedes/overwrites).
- field-assertion-audit: n/a # No encryption, redaction, telemetry payload schema, or permission-gated store field changed.

## Scope

v3.48.0 is a local product batch adding owner-confirmed apply paths and
readiness surfacing. All new behavior is CLI / owner-only and metadata-only:
there is no new agent-facing MCP apply tool, no telemetry schema change, no
permission/governance change, no hard delete, and nothing is auto-published.

This release performs no telemetry schema change, no remote D1/Worker behavior
change (the password rotation helper only writes the Worker secret under an
explicit `-Apply` flag and prints for owner handoff first), no
permission-profile enforcement change, and no user-data migration.

## Changes

- Bumped package metadata and `__version__` to 3.48.0 across `pyproject.toml`,
  `src/piia_engram/__init__.py`, `.mcp/server.json`, `.claude-plugin/plugin.json`,
  `glama.yaml`, and current-state docs (`docs/architecture.md`,
  `docs/cross-tool-guide.md`, `docs/public-facts.json`).
- **Product-use flow hardening**: `engram merge --json` now returns the same
  metadata-only dry-run apply payload as `engram merge apply`, so preview JSON
  never echoes suggestion summaries or stored bodies.
- **Reconcile conflict preview v2**: `engram reconcile conflicts [--json]`
  surfaces conflict counts and match ids only; read-only, metadata-only, and
  never imports/supersedes/overwrites existing decisions.
- **GUI-safe owner actions**: `engram dashboard --json` adds `next_action` plus
  an `actions` list (code/label/command/count/risk, `executes=false`).
- **Telemetry dashboard password rotation helper**:
  `scripts/rotate_telemetry_dashboard_password.ps1` generates/accepts a
  shell-safe `DASH_PASSWORD`, prints it for owner handoff first, and only writes
  the Cloudflare Worker secret when `-Apply` is present.
- **Near-duplicate merge apply (N4)**, **Reconcile import apply (N2)**,
  **Version-chain HEAD surfacing (N5)**, and **Owner dashboard readiness counts
  (D)** as described in CHANGELOG.md — all reversible (soft archive), dry-run by
  default, `--commit --yes` to apply.
- Updated README and README.zh-CN current-state test counts to 2469 passed /
  8 skipped / 2477 collected.

## Evidence Summary

- Public fact sync: `scripts/check_public_fact_sync.py --json` returned OK for
  v3.48.0 with 2469 passed / 8 skipped / 2477 collected and 80/16/64 MCP tools.
- MCP tool counter: `scripts/count_mcp_tools.py --json` returned
  `{"total": 80, "core": 16, "advanced": 64}`.
- Full suite with `ENGRAM_DIR` cleared: `2469 passed, 8 skipped`; collect-only:
  `2477 tests collected`.
- Publish allowlist: all 295 tracked files covered (71 patterns).
- Source sanitize (internal strict): high=0 warn=0.
- Build: `piia_engram-3.48.0.tar.gz` and `piia_engram-3.48.0-py3-none-any.whl`.
- Artifact private-term scan (strict): high=0 warn=0.
- Twine: PASSED for both v3.48.0 artifacts.
- Independent E+ final audit (DeepSeek): APPROVE_WITH_NITS, 0 blocking.
  Non-blocking nits (deferred): (1) password helper passes plaintext via
  `cmd.exe /c` command line under `-Apply` (owner-only, local) — pipe via stdin
  follow-up; (2) a test helper carries an unused `self` parameter.

## Release Notes Draft

### English

Engram v3.48.0 is a local product batch adding owner-confirmed apply paths and
readiness surfacing. Every new capability is CLI / owner-only and metadata-only.

`engram merge --json` now returns the same metadata-only dry-run payload as
`engram merge apply`. `engram reconcile conflicts [--json]` adds a read-only,
metadata-only conflict preview that never mutates existing decisions. `engram
dashboard --json` gains a `next_action` plus a render-only `actions` list
(`executes=false`) so a future GUI has safe metadata without one-click
mutation. Owner-confirmed apply paths for near-duplicate merges and external
memory import remain dry-run by default and reversible (soft archive), applied
only with `--commit --yes`. Version chains now surface the current HEAD in
recall and the resume brief, and the dashboard reports a metadata-only
`readiness` block of pending owner-confirmed applies. A new owner-only
PowerShell helper rotates the telemetry dashboard password, printing it for
handoff and writing the Cloudflare Worker secret only under `-Apply`.

No telemetry schema, remote service behavior, permission enforcement, user-data
migration, or new agent-facing MCP apply tool changes in this release.

### Chinese / 中文

Engram v3.48.0 是一个本地产品批次，新增「所有者确认」的应用路径与就绪度展示。
所有新能力均为 CLI / 所有者专用且仅暴露元数据。

`engram merge --json` 现在返回与 `engram merge apply` 相同的「仅元数据」试运行载荷；
`engram reconcile conflicts [--json]` 新增只读、仅元数据的冲突预览，绝不改写既有决策；
`engram dashboard --json` 新增 `next_action` 与只读 `actions` 列表（`executes=false`），
为未来 GUI 提供安全的元数据而不引入一键改写。近重复合并与外部记忆导入的应用路径默认
试运行、可逆（软归档），仅在 `--commit --yes` 时执行。版本链现在会在 recall 与 resume
摘要中展示当前 HEAD，dashboard 新增仅元数据的 `readiness` 块汇总待所有者确认的应用项。
新增所有者专用 PowerShell 助手用于轮换 telemetry dashboard 密码，先打印交接、仅在
`-Apply` 时写入 Cloudflare Worker secret。

本版本不改 telemetry schema、不改远端服务行为、不启用权限 enforcement、不迁移用户数据，
也不新增面向 agent 的 MCP 应用工具。
