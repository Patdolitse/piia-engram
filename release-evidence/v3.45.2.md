# Release Evidence - v3.45.2

- self-review: passed       # Codex reviewed the v3.45.1 CI failure, entry-point root cause, patch diff, version/docs consistency, and release readiness.
- codex-review: passed      # Codex local review covered pytest console-script parity, package manifest coverage, test evidence, sanitize scans, package build, and twine metadata validation.
- claude-review: passed     # Claude narrow read-only release acceptance for the v3.45.x release path returned PASS with no release blockers; this patch is limited to CI entry-point compatibility.
- tests: passed             # Full suite 2020 passed, 1 skipped, 4 expected engram_core deprecation warnings.
- sanitize: passed          # scripts/release_sanitize_check.py --strict and --commit-messages --strict returned high=0 warn=0.
- publish-allowlist: passed # scripts/check_publish_allowlist.py confirmed tracked public files are covered by .publishallow.
- package-build: passed     # python -m build --outdir E:\Temp\engram-build-v3.45.2 built wheel and sdist.
- twine-check: passed       # python -m twine check E:\Temp\engram-build-v3.45.2\* passed for both artifacts.
- eval-gate: n/a            # This patch changes test import entry-point compatibility only; no retrieval or extraction-quality behavior changed.
- negative-control: n/a     # No security-sensitive governance/encryption/redaction behavior changed in this patch.
- field-assertion-audit: n/a # No security-sensitive storage/encryption/redaction module fields were changed in this patch.

## Scope

Patch release after v3.45.1:

- Fixed the remaining GitHub Actions collection failure caused by the CI using the `pytest` console-script entry point rather than `python -m pytest`.
- `tests/test_cross_tool_resume_benchmark.py` now explicitly places the repository root on `sys.path` before importing the top-level `demos` package.
- Bumped package, MCP registry manifest, Claude plugin manifest, docs, changelogs, and public-positioning tests to 3.45.2.

## Evidence Summary

- CI failure root cause in v3.45.1: `ModuleNotFoundError: No module named 'demos'` still occurred because `pytest` console-script execution did not include the repository root on `sys.path`.
- CI-style regression: running the benchmark test from outside the repository, without `PYTHONPATH`, returned `4 passed`.
- Targeted release-prep suite: `59 passed`.
- Full suite: `2020 passed, 1 skipped, 4 warnings in 182.55s`.
- Working-tree sanitization: `high=0 warn=0`.
- Commit-message sanitization: `high=0 warn=0`.
- Package build: `Successfully built piia_engram-3.45.2.tar.gz and piia_engram-3.45.2-py3-none-any.whl` into `E:\Temp\engram-build-v3.45.2`.
- Sdist verification: `piia_engram-3.45.2/demos/__init__.py` and `piia_engram-3.45.2/demos/cross_tool_resume_benchmark.py` are present.
- Twine: `PASSED` for both `piia_engram-3.45.2-py3-none-any.whl` and `piia_engram-3.45.2.tar.gz`.

## Release Notes Draft

v3.45.2 is a CI entry-point patch release for v3.45.1.

It fixes the remaining clean-checkout GitHub Actions failure by making the cross-tool resume benchmark test robust when invoked through the `pytest` console script, where the repository root may not be present on `sys.path` under the existing `src` layout configuration.

No runtime behavior, security-sensitive governance gate, storage encryption path, or user data migration behavior changed in this patch.

No publishing, tag creation, registry update, or other irreversible action is authorized by this evidence file alone.
